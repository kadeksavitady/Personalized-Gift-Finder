[project]
name = "personalized-gift-finder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "package-lain-yang-kamu-pakai",
]

[tool.uv]
# kalau ada konfigurasi uv lainnya